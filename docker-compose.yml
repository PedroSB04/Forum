version: '3.8'

services:
  db:
    image: postgres:17
    container_name: dev_forum_container
    environment:
      POSTGRES_DB: dev_forum_container
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
    ports:
      - "5432:5432"
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped